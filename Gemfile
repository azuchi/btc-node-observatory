# frozen_string_literal: true

source 'https://rubygems.org'

gem 'async', '~> 2.0'
gem 'bitcoinrb', '~> 1.0'
gem 'sqlite3', '~> 2.0'

# Only needed when using GeoIP (MaxMind GeoLite2)
gem 'maxmind-db', '~> 1.2', require: false

group :development, :test do
  gem 'rake'
  gem 'rspec', '~> 3.0'
end
